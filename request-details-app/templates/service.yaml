apiVersion: v1
kind: Service
metadata:
  name: {{ include "request-details-app.fullname" . }}
  labels:
    {{- include "request-details-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "request-details-app.selectorLabels" . | nindent 4 }}
