apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "request-details-app.fullname" . }}-test-connection"
  labels:
    {{- include "request-details-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "request-details-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
